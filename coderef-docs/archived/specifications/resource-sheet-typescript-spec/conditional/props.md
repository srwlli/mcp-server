# Props Module

**Type:** Conditional
**Triggers:** React/Vue components with props/parameters
**Applies to:** ui/components, ui/widgets, ui/pages
**Auto-fill:** Prop names, types, required/optional, defaults
**Manual:** Prop design rationale, validation, usage examples

---

## Section: Props Reference

### Props Interface

{{AUTO_FILL: element.metadata.props}}

```typescript
interface {{element.name}}Props {
  {{#each element.metadata.props}}
  {{this.name}}{{#unless this.required}}?{{/unless}}: {{this.type}};
  {{/each}}
}
```

### Props Table

| Prop | Type | Required | Default | Description |
|------|------|----------|---------|-------------|
{{#each element.metadata.props}}
| {{this.name}} | `{{this.type}}` | {{#if this.required}}Yes{{else}}No{{/if}} | {{this.default}} | {{this.description}} |
{{/each}}

{{MANUAL: Explain prop design decisions:
- Why these props? Why required vs optional?
- Could any props be derived or merged?
- Are there too many props (> 10)?}}

### Prop Validation

{{AUTO_FILL: element.metadata.propValidation}}

{{MANUAL: Explain validation strategy:
- Runtime validation (PropTypes, Zod)?
- TypeScript compile-time only?
- Custom validation logic?
- Error handling for invalid props?}}

### Usage Examples

{{MANUAL: Provide 2-3 examples:

**Basic Usage:**
```tsx
<{{element.name}}
  prop1={value1}
  prop2={value2}
/>
```

**Advanced Usage:**
```tsx
<{{element.name}}
  prop1={value1}
  prop2={value2}
  optionalProp={value3}
  onEvent={(data) => handleEvent(data)}
/>
```}}

---

## Metadata

**Generated by:** Resource Sheet MCP Tool
**Module:** props (conditional)
**Version:** 1.0.0
