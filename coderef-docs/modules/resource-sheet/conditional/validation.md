# Validation Module

**Type:** Conditional
**Triggers:** Zod, Yup, validators, schema validation
**Applies to:** data/schemas, data/validators, services/api-endpoints
**Auto-fill:** Validation schemas, rules
**Manual:** Validation strategy, error messages

---

## Section: Data Validation

### Validation Schema

{{AUTO_FILL: element.metadata.validationSchema}}

```typescript
const schema = z.object({
  {{#each element.metadata.validationFields}}
  {{this.name}}: z.{{this.type}}(){{#if this.rules}}.{{this.rules}}(){{/if}},
  {{/each}}
});
```

{{MANUAL: Explain validation rules and why they exist}}

### Validation Strategy

{{MANUAL: Explain when validation occurs:
- Client-side or server-side?
- On input or on submit?
- Real-time or deferred?}}

### Error Messages

{{MANUAL: Document validation error messages:
```typescript
{
  "field.required": "This field is required",
  "field.min": "Minimum length is X characters"
}
```}}

---

## Metadata

**Generated by:** Resource Sheet MCP Tool
**Module:** validation (conditional)
**Version:** 1.0.0
