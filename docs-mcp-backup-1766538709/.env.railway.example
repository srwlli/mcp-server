# MCP HTTP Server Configuration for Railway.app Deployment

# ============================================================================
# REQUIRED SETTINGS
# ============================================================================

# Port binding (Railway sets this automatically, default 5000 for local)
PORT=8000

# Flask environment mode
FLASK_ENV=production

# Standalone mode (required for Railway deployment)
# Set to 'true' to run docs-mcp independently without sibling MCP servers
STANDALONE_MODE=true

# ============================================================================
# SECURITY SETTINGS (Required for Production)
# ============================================================================

# API Key for authentication (REQUIRED for production)
# Generate a strong random key: openssl rand -hex 32
# All requests must include X-API-Key header with this value
MCP_API_KEY=your-secure-api-key-here

# CORS Allowed Origins (optional, default '*')
# Set to restrict which domains can make requests
# Use comma-separated list for multiple origins
# Examples:
#   ALLOWED_ORIGINS=*                                    (allow all - dev only)
#   ALLOWED_ORIGINS=https://chat.openai.com             (single origin)
#   ALLOWED_ORIGINS=https://chat.openai.com,https://myapp.com  (multiple)
ALLOWED_ORIGINS=*

# ============================================================================
# OPTIONAL SETTINGS
# ============================================================================

# Optional: Path to project for MCP tools to analyze
# Leave blank to use current working directory
MCP_PROJECT_PATH=

# Optional: MCP server socket/stdio path if reusing existing server
# Leave blank to use local tool_handlers directly
MCP_SERVER_PATH=
