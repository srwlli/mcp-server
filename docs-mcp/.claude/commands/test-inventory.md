Discover test files, detect test frameworks (pytest, unittest, jest, mocha, vitest), analyze coverage metrics if available, and identify untested source files. Generates comprehensive test infrastructure manifest.

Call the `mcp__docs-mcp__test_inventory` tool with the current working directory as the project_path.
