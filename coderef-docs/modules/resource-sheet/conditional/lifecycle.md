# Lifecycle Module

**Type:** Conditional
**Triggers:** useEffect, componentDidMount, onMounted, lifecycle hooks
**Applies to:** ui/components, ui/widgets, state/hooks
**Auto-fill:** Effect dependencies, cleanup functions
**Manual:** Lifecycle rationale, side effects, timing

---

## Section: Component Lifecycle

### Initialization

{{AUTO_FILL: element.metadata.hooks.filter(h => h.phase === 'mount')}}

{{MANUAL: Explain what happens on mount:
- What setup is performed?
- Are there async operations?
- What resources are allocated?}}

### Updates

{{AUTO_FILL: element.metadata.hooks.filter(h => h.phase === 'update')}}

{{MANUAL: Explain update behavior:
- What triggers re-renders?
- What re-runs on updates?
- Are updates batched?}}

### Cleanup

{{AUTO_FILL: element.metadata.hooks.filter(h => h.hasCleanup)}}

{{MANUAL: Explain cleanup:
- What resources are released?
- Are listeners removed?
- Is state persisted before unmount?}}

---

## Metadata

**Generated by:** Resource Sheet MCP Tool
**Module:** lifecycle (conditional)
**Version:** 1.0.0
